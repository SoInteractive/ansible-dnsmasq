# {{ ansible_managed }}
[Unit]
Description=Dnsmasq metrics exporter
After=network-online.target

[Service]
Type=simple
ExecReload=/bin/kill -HUP $MAINPID
ExecStart=/opt/dnsmasq_exporter

SyslogIdentifier=dnsmasq_exporter
Restart=always

[Install]
WantedBy=multi-user.target
